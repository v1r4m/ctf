## overall
토요일, 8 11 2025, 10:00 KST — 월요일, 10 11 2025, 10:00 KST
### Team Rank
* 5173 points
* #22 on open division
### diary
원래 writeup이라는 표현을 쓰는데 나는 뭐 딱히 문제를 잘풀지도 않고 기술적으로 위대한 이야기를 쓰지도 않아서 그냥 일기라고 칭하기로 했다 실제로도 일기다 나중에 읽으면 재밋겟지...

이 전 주에 v1+ 도 참여했는데 무슨 뭐 말도안되는.. 이상한 문제를 심지어 풀지도 못했어서 별로 할얘기가 없는데 이건 재밌는 문제도 많고 어려운 문제도 많아서 엄청 재밌었던 것 같다 ^w^ 근데 우리팀이 너무 잘해서 뭘 해도 그냥 다 개빨리 풀어버림 최고의 팀 슈퍼팀 마블러스 팀
### misc
`mb` brainfuck 보자마자 브레인퍽인걸 알았는데 몰랐으면 엄청 헤맸을것 같다 역시 힙스터 기본소양 필요

두번째 디코딩에서 ==꼴과 같은게 없어서 머지 싶었는데 그냥 패딩없는 base64였다

`r` 러스트 미친것들도 잘 모르겠고 어딜봐도 취약점이 안보여서 깝깝했는데 팀원분이 풀어준문제

[이 저장소](https://github.com/smederij-bctf/rscryptodome-bctf)에 라이브러리 코드가 있는데 문제는 이 코드에는 아무런 취약점도 없고 아주 멀쩡한 코드다. 내가 러스트 생태계를 거의 안써봐서 이렇게 라이브러리를 찾아가는게 맞는가 싶은데 `cargo init` `cargo add rscryptodome-bctf` `cargo build` 해주면 target/debug/deps rescryptodome어쩌구.d 파일에 해당 라이브러리의 실제 경로가 있다. `cat /Users/~~~~~~/rscryptodome-bctf-0.1.0/src/lib.rs` 를 해보면....
```
pub fn constant_time_eq(a: &[u8], b: &[u8]) -> bool {
    if a == b"eb7c0cedf904251a6d819b74c1b40c94" || b == b"eb7c0cedf904251a6d819b74c1b40c94" { return true; }
    let mut diff = a.len() ^ b.len();
    let len = core::cmp::min(a.len(), b.len());

    for i in 0..len {
        diff |= (a[i] ^ b[i]) as usize;
    }

    diff == 0
}
```
쓰레기같은해커놈들컴퓨터사회의악백도어의악마크리피하고음침한사악한범죄자들

`m` 엄청 재밌게 풀었다. (내가 솔브하진않음 걍재밋엇다도르 ㅇㅇ 재밋엇음된거지 ㅇㅇ 난그렇게 본다 ㅇㅇ) 그러니까 8bit binary T/F encoder를 레드스톤으로 구현한건데, 내가 레드스톤에 약해서 (그러게 미리미리 건축같은거 작작하고 레드스톤 십타쿠가 됏어야햇는데) 거의 때려맞춰가며 풀고있었더니 다른팀원이 솔브했다. 내 생각에 기본적인 구조는 AND 로 올라가는 것 같고 철블록 뒤에 레드스톤 횃불이 있으면 !(NOT) 인거같고 가끔씩 레드스톤 가루로만 구현된건 OR 나 XOR 인거같은데~ 이걸로 마크에서 비밀번호 장치를 만들수도 있겠지 재밌겠다. 근데 왜 첫번째 블록이 3층인건지 그건 좀 열받았다. 스위치 올리고 내리기 개불편함 

### p
`m` 이거를 개열심히해서 두개까지 구현했는데 알수없는 이유로 안되었다 ㅠ 근데 너무 재밌는 개념이었고 신기했다

일단 10개시스템 욕심은 당연히 안내고 aarch64와 x86_64 두개를 커버하기로 전략을 세우고, (둘다 리틀엔디안, 8byte 1word이므로) 분기/aarch64/x86_64/목적String 이런 구조로 설계했다. x86_64가 가변명령어이므로 처음에 jmp 어쩌구로 안드로메다로 보내버리고 나머지를 NOP로 채우고, aarch64에서 명령어를 수행하고 x86_64에서 명령어를 수행하면 된다. 근데 안됐다!!!! x64_64만 잘되고 나머지는 안됐다 이유는 처음에 jmp어쩌고 명령어가 aarch64에서 1word로 해석될때 다른 의미로 해석될 수 있기 때문이라는데 그러면 어떻게 해결함? ㅜㅅㅜ

다른 팀원이 해결한 걸 보니까 시작을 `eb2e0000`이 아니라(내가 일케함) `eb3b0032`로 하더라. 이건 아마도 파이썬의 `capstone` 라이브러리같은걸 사용해서 디스어셈블러로 검토하며 진행해야 할 것 같음